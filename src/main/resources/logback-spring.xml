<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="30 seconds">

    <property name="LOG_PATH" value="logs"/>
    <property name="LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"/>

    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- Application Log Appender -->
    <appender name="APP_LOG" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/Payment_%d{yy_MM_dd}.log</fileNamePattern>
            <!-- Keep all files forever -->
        </rollingPolicy>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- System Log Appender -->
    <appender name="SYS_LOG" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/System_%d{yy_MM_dd}.log</fileNamePattern>
            <!-- Keep all files forever -->
        </rollingPolicy>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- Application Logger (com.az.payment and all subpackages) -->
    <logger name="com.az.payment" level="DEBUG" additivity="false">
        <appender-ref ref="APP_LOG"/>
        <appender-ref ref="CONSOLE"/>
    </logger>

    <!-- Root Logger (everything else) -->
    <root level="INFO">
        <appender-ref ref="SYS_LOG"/>
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>
